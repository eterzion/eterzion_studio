# Generated by make_video_fixtures.py — real FFmpeg output, rebuilt on demand
# rather than committed. Keeping a few hundred KB of binary per fixture in git
# would also pin the tests to whichever FFmpeg produced them.
*.mp4
*.mkv
*.webm
*.mov
